import { addTally } from './add-tally';
import { removeTally } from './remove-tally';
import { getTally } from './get-tally';

const db = {
  addTally,
  removeTally,
  getTally,
};

export default db;
